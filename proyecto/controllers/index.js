const users = require('./users');
const areas = require('./area');
const cargos = require('./cargo');
const roles = require('./rol.js');

module.exports = {
    users,
    areas,
    cargos,
    roles,
};